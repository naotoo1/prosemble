# DISABLED for PR #56 - Nix version incompatibility
# This workflow will be re-enabled after the PR is merged and Nix is updated
# Original filename: build-test-ubuntu.yml

name: Test Python Versions and Container Builds (DISABLED)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is temporarily disabled"
