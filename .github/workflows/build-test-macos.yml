name: Test macOS Builds

# TEMPORARILY DISABLED for PR - only manual runs allowed  
# Will be re-enabled after Nix version update
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is temporarily disabled - manual trigger only"
