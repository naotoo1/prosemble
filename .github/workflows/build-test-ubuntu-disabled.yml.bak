# DISABLED: This workflow has been temporarily disabled for testing PRs
# It will be re-enabled after Nix version is updated in the main branch
# Original file: build-test-ubuntu.yml

name: Test Python Versions and Container Builds (DISABLED)

on:
  workflow_dispatch:
  # Disabled for now - uncomment when ready
  # push:
  #   branches: [ main ]
  #   paths:
  #     - 'devenv.nix'
  #     - 'devenv.lock'
  #     - '.github/workflows/**'
  # pull_request:
  #   branches: [ main ]
  #   paths:
  #     - 'devenv.nix'
  #     - 'devenv.lock'
  #     - '.github/workflows/build-test-ubuntu.yml'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled"
